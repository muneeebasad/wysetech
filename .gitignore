# Dependencies
node_modules/

# Next.js build output
.next/
out/

# Environment variables — never commit secrets
.env
.env.local
.env.*.local

# OS / editor noise
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/

# Claude Code local settings
.claude/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Docker volumes (local bind-mount data — managed on the server)
# content/ and public/team/ are committed as seed data only;
# live edits on the server are kept via Docker volumes.
